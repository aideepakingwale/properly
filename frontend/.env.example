# Frontend environment variables
# Vite exposes only variables prefixed with VITE_ to the browser bundle.
#
# LOCAL DEV:  Leave blank — Vite dev server proxies /api → localhost:3001
# RENDER:     Set this in the Render Static Site dashboard to your API URL:
#             https://properly-api.onrender.com/api
#
VITE_API_URL=
